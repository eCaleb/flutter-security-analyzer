# Exclude intentional test fixtures in the samples directory from secret scanning.
# These files contain deliberately vulnerable code for testing the Flutter Security Scanner.
paths-ignore:
  - "samples/**"
  - "tests/**"
